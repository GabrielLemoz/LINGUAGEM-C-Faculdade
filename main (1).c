#include <stdio.h>

int main() {
    //declara uma variavel para armazenar número
    int numero;

    //pede ao usuario para digitar um numero
        printf("digite um numero inteiro: ");

    //le o numero digitado pelo usuario 
    scanf("%d", &numero);

    // exibe
    printf("voce digitou: %d\n", numero);

    return 0;
}
